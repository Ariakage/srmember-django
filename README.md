# srmember-django
SR inner-member system, developed with Django.

## Development

Install dependencies and run Django commands with uv:

```bash
uv sync
uv run python manage.py check
uv run python manage.py runserver
```
